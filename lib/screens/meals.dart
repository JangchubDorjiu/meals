import 'package:flutter/material.dart';

class MealsScreen extends StatelessWidget {
  const MealsScreen({super.key, required List meals, required String title});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}